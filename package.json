{
  "name": "ai-debugging-tutor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "demo:tutor": "tsx packages/tutor-core/src/demo.ts",
    "test": "vitest run",
    "web": "npm run dev -w @ai-debugging-tutor/web"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
